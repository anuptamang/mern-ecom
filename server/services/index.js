/**
 * Business logic services index
 * Export all services from here for better organization
 * 
 * Services contain business logic that is reusable across controllers
 */

// Example service exports would go here
// Services should contain business logic separated from controllers

export const exampleService = {
  // Business logic methods
};
